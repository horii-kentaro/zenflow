services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: zenflow
      POSTGRES_PASSWORD: zenflow_dev
      POSTGRES_DB: zenflow
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
